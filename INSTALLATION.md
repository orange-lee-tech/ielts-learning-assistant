# IELTS Learning Assistant 本机安装说明

本插件在安装后完全在浏览器本机运行，不需要 VPN、GitHub、开发终端、账号或远程服务器。

## 一、Chrome / Edge 长期加载

1. 解压收到的 Chrome 或 Edge 安装包，并把文件夹放到长期保存的位置，例如：

   ```text
   D:\Browser Extensions\IELTS Learning Assistant
   ```

2. 打开扩展管理页面：

   - Chrome：`chrome://extensions`
   - Edge：`edge://extensions`

3. 打开右上角或左侧的“开发者模式”。
4. 点击“加载已解压的扩展程序”。
5. 选择刚才解压出来、内部含有 `manifest.json` 的文件夹。
6. 在浏览器工具栏的扩展菜单中固定 IELTS Learning Assistant 图标。
7. 打开普通网页，点击插件图标，确认侧栏正常出现。

只要不移动或删除该文件夹，关闭并重新打开浏览器后插件仍会保留。

## 二、Firefox 测试安装

Firefox 版本需为 140 或更高。Firefox 的未签名扩展只能临时安装，重启浏览器后会消失；长期安装需要经过 Mozilla 签名。

1. 打开 `about:debugging#/runtime/this-firefox`。
2. 点击“临时载入附加组件”。
3. 选择 Firefox 构建文件夹中的 `manifest.json`。
4. 点击插件图标打开侧栏。

## 三、日常使用

1. 打开普通网页。
2. 选中英文文本。
3. 点击插件图标打开侧栏。
4. 填写笔记、错因和标签，然后保存。
5. 在 Library 查看最近笔记。
6. 点击“导出 TXT”把全部笔记保存到电脑。

TXT 使用 UTF-8 编码并包含兼容 Windows 的 BOM，可直接用记事本、WPS 或 Word 打开。

## 四、更新插件

1. 先备份旧文件夹；如有重要笔记，也先在 Library 导出 TXT。
2. 用新版文件替换旧插件文件夹中的内容。
3. 回到浏览器扩展管理页。
4. 点击插件卡片上的“重新加载”按钮。
5. 打开普通网页，检查侧栏、保存和导出功能。

浏览器的本地扩展存储通常会在重新加载后保留，但升级前导出 TXT 是更稳妥的做法。

## 五、常见情况

- `chrome://`、`edge://`、扩展商店和部分内置页面禁止内容脚本运行，因此无法捕获选中文字；这不是插件损坏。
- 首次安装后，已经打开的网页可能需要刷新一次。
- 不要把安装文件夹放在临时下载目录，也不要在加载后移动它。
- Chrome / Edge 的“开发者模式扩展”提示属于浏览器机制；通过应用商店正式发布后才能避免。
