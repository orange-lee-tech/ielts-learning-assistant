# IELTS Learning Assistant 本机安装说明

本插件安装后完全在浏览器本机运行，不需要 VPN、GitHub、开发终端、账号或远程服务器。

## 一、Chrome / Edge 长期加载

1. 解压 Chrome 或 Edge 安装包，并把文件夹放到长期保存的位置。
2. 打开 `chrome://extensions` 或 `edge://extensions`。
3. 开启“开发者模式”，点击“加载已解压的扩展程序”。
4. 选择内部含有 `manifest.json` 的文件夹。
5. 固定 IELTS Learning Assistant 图标，在普通网页中点击图标打开侧栏。

只要不移动或删除该文件夹，关闭并重新打开浏览器后插件仍会保留。

## 二、Firefox 测试安装

Firefox 版本需为 140 或更高。未签名扩展只能临时安装，重启浏览器后会消失；长期安装需要 Mozilla 签名。

1. 打开 `about:debugging#/runtime/this-firefox`。
2. 点击“临时载入附加组件”。
3. 选择 Firefox 构建文件夹中的 `manifest.json`。
4. 点击插件图标打开侧栏。

## 三、日常使用

1. 在练习网页中选中英文原句并打开插件侧栏。
2. 在 Capture 选择剑雅年份。
3. 填写词汇、笔记、错因和标签；自定义标签可直接添加或移除。
4. 点击“保存笔记”。
5. 在 Library 按年份查看“词汇—原句—笔记”的完整关系。
6. 每条记录都可复制、编辑或删除；编辑时会返回 Capture，点击“保存修改”更新原记录。
7. 使用“复制文档”复制全部 Markdown，或“导出 Markdown”保存 `.md` 文件。

## 四、从上一安装包升级

1. 在旧版 Library 先导出 TXT 备份。
2. 用新版文件替换原插件文件夹中的内容。
3. 回到浏览器扩展管理页，点击插件卡片上的“重新加载”。
4. 旧笔记会继续保留，并显示在“未分类旧笔记”中。
5. 点击旧笔记的“编辑”，补充剑雅年份和词汇后保存，即可进入对应年份分类。

## 五、删除与备份

- 单条删除和“删除全部记录”都会先弹出确认。
- 删除无法在插件内撤销，批量删除前请先导出 Markdown。
- Markdown 使用 UTF-8 BOM，可用记事本、WPS、Word、Typora 或 VS Code 打开。

## 六、常见情况

- `chrome://`、`edge://`、扩展商店和部分内置页面禁止捕获选中文字，这不是插件损坏。
- 首次安装后，已经打开的网页可能需要刷新一次。
- 不要把安装文件夹放在临时下载目录，也不要在加载后移动它。
- Chrome / Edge 的“开发者模式扩展”提示属于浏览器机制。
